name: Deploy documentation

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build doxygen

      - name: Configure documentation build
        run: >
          cmake -S "${{ github.workspace }}"
          -B "${{ github.workspace }}/build-pages"
          -G Ninja
          -DBUILD_TESTING=OFF
          -DDTA_BUILD_PYTHON_BINDINGS=OFF
          -DDTA_BUILD_DOCS=ON

      - name: Build documentation
        run: cmake --build "${{ github.workspace }}/build-pages" --target dta_docs

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ github.workspace }}/build-pages/doc/html

  deploy-pages:
    needs: build-docs
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
